<p>
  you're now at <b>form.html.erb (reply)</b>
</p>

<%= form_for(@reply) do |f| %>
  <% if @reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reply.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
      <% @reply.errors.full_replies.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :reply %><br />
    <%= f.text_area :reply%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
